// Deliberately unsafe synthetic source for the static security demo.
// This module is not loaded by index.html. Never copy this pattern into an app.
export function renderMessage(container, message) {
  container.innerHTML = message;
}
